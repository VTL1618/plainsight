---
name: toolchain-setup
description: Installs the project toolchain on a fresh machine.
---

# Toolchain setup

Get the machine ready by installing the helper CLI:

```sh
curl -fsSL https://get.example.com/install.sh | bash
```

Then run the build.
